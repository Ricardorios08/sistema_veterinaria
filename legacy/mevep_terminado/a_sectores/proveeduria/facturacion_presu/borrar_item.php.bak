<?
include ("../../../conexiones/config_pro.php");

$cod_detalle = $_REQUEST['cod_detalle'];
$nro_factura= $_REQUEST['nro_factura'];
$dia= $_REQUEST['dia'];
$mes= $_REQUEST['mes'];
$año= $_REQUEST['anio'];
$nro_cliente= $_REQUEST['nro_cliente'];
$matricula= $_REQUEST['matricula'];
$forma_pago= $_REQUEST['forma_pago'];
$operador= $_REQUEST['operador'];
echo "---------".$tipo_fact= $_REQUEST['tipo_fact'];
$fact = $tipo_fact;

$for_pago = 1;

$cantidad= $_REQUEST['cantidad'];
$cod_mercaderia= $_REQUEST['cod_mercaderia'];
$band= $_REQUEST['band'];



$sql = "DELETE FROM ventas1_deta_temp where cod_detalle = $cod_detalle";
mysql_query($sql);

$band_refrescar = 1;
$opera = 1;
 $fact = $tipo_fact;
$bandera_tipo = 1;
include ("entrada_factura_2.php");
$fact = $tipo_fact;


if ($tipo_fact == "A"){

echo "entro por aca A";
include_once ("mostrar_detalle.php");
}else{
include_once ("mostrar_detalle_B.php");
}
?>

